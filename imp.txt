@import url("https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css");

<a href="star-fill.svg">

/*const labels = document.querySelectorAll('.form-control label')

labels.forEach(label => {
    label.innerHTML = label.innerText
    .split('')
    .map((letter, idx) => `<span style="transition-delay:${idx * 50}ms">${letter}</span>`)
    .join('')
}) */

/* $(window).on("hashchange", function () {
    if (location.hash.slice(1) == "register") {
    
        $("#register").addClass("active");
    } else {
        
        $("#register").removeClass("active");
    }
});
$(window).trigger("hashchange"); */

function validateRegisterForm() {
    var name = document.getElementById("fullName").value;
    var mail = document.getElementById("regEmail").value;
    var password = document.getElementById("regPassword").value;
    var phone = document.getElementById("phoneNumber").value;
    var address = document.getElementById("fAddress").value;

    if (name == "" || mail == "" || password == "" || phone == "" || address == "") {
        document.getElementById("errorMsg").innerHTML = "Please fill the required fields"
        return false;
    }

    else if (password.length < 8) {
        document.getElementById("errorMsg").innerHTML = "Your password must include atleast 8 characters"
        return false;
    }
    else {
        alert("Successfully signed up");
        window.location="login.html";
        return true;
    }
}




<div id="errorMsg"></div>
  <div class="container">
    <h1>Register to the BYK Book Shop</h1>
    <form id="register" name="registerForm" action="/" method="POST">
      <div class="form-control">
        <input type="text" name="name" id="fullName" required>
        <label>Name</label>
      </div>
      <div class="form-control">
        <input type="text" name="email" id="regEmail" required>
        <label>Email</label>
      </div>
      <div class="form-control">
        <input type="password" name="password" id="regPassword" required>
        <label>Password</label>
      </div>
      <div class="form-control">
        <input type="phone" name="phone" id="phoneNumber" required>
        <label>Phone Number</label>
      </div>
      <div class="form-control">
        <input type="text" name="address" id="fAddress" required>
        <label>Address</label>
      </div>
      <button class="btn">Register</button>
      <p class="text">Already have an account? <a href="login.html">Login</a></p>
    </form>

shop 

    <section id="page-header">
    <h2>#StaySafe #StayHome</h2>
    <p>Save more with coupons and up to 60% off</p>
    
  </section>



  <form action="" class="search-form">
      <input type="search" id="search box" placeholder="Search books">
      <label for="search-box" class="bi bi-search"></label>
    </form>

    #header search-form {
    position: absolute;
    top: 110%; right: 2rem;
    max-width: 50rem;
    height: 4rem;
    background: #fff;
    border-radius: .5rem;
    overflow: hidden;
    display: flex;
    align-items: center;
}

#header search-form label {
    font-size: 2.2rem;
    padding-right: 1.5rem;
    color: black;
    cursor: :pointer;
}




setTimeout(function(){submitError.style.display = 'none';}, 3000);